name: Set up Gradle build
description: Configure Java 21 and Gradle for Kotlin builds.

runs:
  using: composite
  steps:
    - name: Set up Java
      uses: actions/setup-java@v5
      with:
        distribution: zulu
        java-version: "21"

    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v6
